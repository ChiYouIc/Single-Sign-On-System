# 服务配置
server:
  servlet:
    context-path: /
  port: 8501

# spring 配置
spring:
  # thymeleaf
  thymeleaf:
    prefix: classpath:/templates/

    check-template-location: true
    suffix: .html
    encoding: UTF-8
    mode: HTML5
    cache: false
    servlet:
      content-type: text/html
